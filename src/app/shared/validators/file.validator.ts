/**
 * File validator utility functions will be written here. These functions will be used to validate the file type, size, and other properties.
 * @module file.validator
 * @author Raiyan Jiyon
 * @version 1.0.0
 * @since 2026-07-06
 * @description This module contains utility functions for validating files.
 * @example
 * import { validateFileType, validateFileSize } from './file.validator';
 *
 * const isValidType = validateFileType(file, ['image/jpeg', 'image/png']);
 * const isValidSize = validateFileSize(file, 5 * 1024 * 1024); // 5MB
 */
